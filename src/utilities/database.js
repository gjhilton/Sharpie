const allSources = db => db.sources

const allGraphSets = db => db.graphSets

const source = sources, key => sources[key]

const graphSet = graphSets, key => graphSets[key]

const characters = graphSet => graphset.graphs 